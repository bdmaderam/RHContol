export const generarPDF = (data, nombreArchivo) => {
  console.log(`Simulando generación de PDF: ${nombreArchivo}`);
  // Implementación real requeriría una librería como jsPDF
};

export const generarExcel = (data, nombreArchivo) => {
  console.log(`Simulando generación de Excel: ${nombreArchivo}`);
  // Implementación real requeriría una librería como xlsx
};